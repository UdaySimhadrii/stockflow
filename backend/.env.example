# Server
PORT=5000
NODE_ENV=development

# Database (SQLite file path, relative to backend/)
DB_STORAGE=./src/database/stockflow.sqlite

# JWT
JWT_SECRET=change_this_to_a_long_random_secret_in_production
JWT_EXPIRES_IN=7d

# CORS
CLIENT_ORIGIN=http://localhost:5173
